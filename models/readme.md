# Models

This folder contains the future data models for AuditNIST Pro.

Planned models:

- `auditSession.js` → full audit session structure
- `control.js` → individual control structure
- `evaluation.js` → control evaluation structure

These files are currently placeholders for the future modular architecture.