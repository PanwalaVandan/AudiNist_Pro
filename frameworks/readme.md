# Framework Adapters

This folder contains framework adapters for AuditNIST Pro.

Current status:
- The standalone UI mainly relies on SCF-derived JSON data.
- These adapters are kept as architectural references and for future modular evolution.

Potential future uses:
- AuditEngine integration
- framework-specific category handling
- AI context per framework
- fallback logic independent from SCF JSON